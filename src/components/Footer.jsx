export default function Footer() {
  return (
    <footer className="px-6 md:px-20 py-16 border-t border-black dark:border-gray-600">
      <p className="text-sm">Built with React + Tailwind CSS.</p>
      <p className="mt-3 text-xs opacity-50 font-mono">Birthday loading...</p>
      <p className="mt-3 text-xs opacity-50 font-mono">
        // if (age === 12) {"{"} unlockLegendaryMode(); {"}"}
      </p>
      <p className="text-sm mt-5">Happy 12th Birthday 🎉</p>
      <p className="mt-8 text-xs opacity-50 font-mono">Find the secret code</p>
      <p className="mt-3 text-xs opacity-40 font-mono">
        // hint : developers usually prefer the dark.
      </p>
    </footer>
  );
}
